--1
SELECT *
FROM Водители
WHERE стаж_водителя NOT IN (3,5,6,10) 
					AND место_прописки LIKE 'eee' 
--2
SELECT
FROM
LEFT JOIN 

SELECT d.PassportID, e.Rout
FROM Drivers d
		LEFT JOIN Expediter e ON d.id_drivers=ec.id_drivers
WHERE Rout < 1

SELECT рег_номер, время_прибытия, время_отбытия, пункт_назначения 
FROM автомобили a 
JOIN диспетчерская d
ON a.кодавто = d.кодавто
GROUP BY Name
WHERE год_выпуска = 2015 